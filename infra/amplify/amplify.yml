version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@11
        - pnpm install --frozen-lockfile
    build:
      commands:
        - pnpm --filter @makaan/web build
  artifacts:
    baseDirectory: apps/web/dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/web/node_modules/**/*
